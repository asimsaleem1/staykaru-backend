specVersion: "v1"
serviceId: "staykaru-backend"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "staykaru-backend-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
